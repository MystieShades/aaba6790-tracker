{
  "name": "aaba6790-tracker",
  "version": "1.0.0",
  "description": "My first nodeJS server",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "prestart": "parcel build ./public/index.html",
    "dev": "parcel serve ./public/index.html"
  },
  "author": "Ashley",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
